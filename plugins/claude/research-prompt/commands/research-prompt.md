Generate a Deep Research prompt artifact for the current project.

Run the research-prompt helper with `--harness claude`, pass the user's research question as `--problem`, and include explicitly mentioned project paths with repeated `--path` arguments.
